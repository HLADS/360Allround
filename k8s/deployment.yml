apiVersion: apps/v1
kind: Deployment
metadata:
  name: threesixtyallround-web
  namespace: threesixtyallround-web-ns
  labels:
    app: threesixtyallround-web
spec:
  strategy:
    rollingUpdate:
      maxSurge: 30%
      maxUnavailable: 0
    type: RollingUpdate
  replicas: 1
  selector:
    matchLabels:
      app: threesixtyallround-web
  template:
    metadata:
      labels:
        app: threesixtyallround-web
    spec:
      containers:
      - name: threesixtyallround-web
        image: hlads/threesixtyallround
        imagePullPolicy: Always
        resources:
          limits:
            memory: 350Mi
            cpu: "0.5"
          requests:
            memory: 300Mi
            cpu: "0.25"
        ports:
        - containerPort: 80